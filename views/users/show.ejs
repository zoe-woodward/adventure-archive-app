<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.username %>'s Adventures</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= user.username %>'s Adventures</h1>
    <% if (user.adventures.length > 0) { %>
        <ul>
            <% user.adventures.forEach(adventure => { %>
                <li>
                   <%= adventure.destination %>
                   <%= adventure.memories%>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>This user hasn't yet embarked on their first adventure. Check back soon!</p>
    <% } %>
</body>
</html>